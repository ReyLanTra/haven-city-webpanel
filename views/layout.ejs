<!DOCTYPE html>
<html lang="id">
<link rel="stylesheet" href="/css/style.css">
<head>
  <meta charset="UTF-8">
  <title><%= title || 'Haven City Panel' %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white font-sans">
  <nav class="bg-gray-800 px-6 py-4 shadow-md flex justify-between">
    <h1 class="text-xl font-bold">Haven City Panel</h1>
    <% if (user) { %>
      <div class="flex items-center gap-3">
        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" class="w-8 h-8 rounded-full" />
        <span><%= user.username %></span>
        <a href="/auth/logout" class="text-red-400 ml-4">Logout</a>
      </div>
    <% } %>
  </nav>

  <main class="p-6">
    <%- body %>
  </main>
</body>
</html>